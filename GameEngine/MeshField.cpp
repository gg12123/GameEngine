#include "MeshField.h"
#include "World.h"

std::string MeshField::Instantiate( const World& world ) const
{
   // use Value() to load mesh asset from asset loader, copy it, assign it a unique name,
   // pass it back to the asset loader, return the new name.
   throw std::exception( "no imp" );
}